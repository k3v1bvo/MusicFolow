{
  "name": "musicfolow",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "cd frontend && npm install && npm run build && cd ../backend && npm install && rm -rf public && cp -r ../frontend/dist/frontend public",
    "start": "cd backend && node server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": ""
}
